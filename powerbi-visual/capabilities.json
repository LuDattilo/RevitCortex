{
  "dataRoles": [
    {
      "name": "elementIds",
      "kind": "Grouping",
      "displayName": "Element ID",
      "description": "Map the ElementId column from the Elements table"
    },
    {
      "name": "colorBy",
      "kind": "Grouping",
      "displayName": "Color by",
      "description": "Optional. Drop any categorical field (e.g. Discipline, Family, Status, Workset). Each distinct value gets a deterministic color from the palette, applied as an override in Revit. Hex strings like '#E53935' are honored as-is."
    }
  ],
  "dataViewMappings": [
    {
      "table": {
        "rows": {
          "select": [
            { "for": { "in": "elementIds" } },
            { "for": { "in": "colorBy" } }
          ],
          "dataReductionAlgorithm": {
            "top": { "count": 30000 }
          }
        }
      }
    }
  ],
  "supportsHighlight": true,
  "objects": {},
  "privileges": [
    {
      "name": "WebAccess",
      "essential": true,
      "parameters": [
        "http://localhost:27016"
      ]
    }
  ]
}
